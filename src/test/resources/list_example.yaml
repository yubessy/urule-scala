- pattern:
    host: '^example\.com$'
  return:
    tag: zero

- pattern:
    host: '^\w+\.example\.com$'
  return:
    tag: one

- pattern:
    host: '^\w+\.\w+\.example\.com$'
  return:
    tag: two
